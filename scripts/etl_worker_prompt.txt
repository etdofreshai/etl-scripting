Continue implementing the ETL bootstrap compiler/interpreter project in /opt/data/workspace/etl-scripting as a continuous implementation loop.

Primary mission:
- Keep making real, incremental implementation progress toward a complete ETL toolchain.

Execution policy for every iteration:
1. Inspect current repo state and pick exactly one highest-leverage next slice of work.
2. Prefer TDD: add or update a failing test first whenever practical.
3. Implement the smallest coherent change that makes the test pass.
4. Run verification (`cargo fmt && cargo check && cargo test`).
5. Commit and push only if progress is real and the repo is green.
6. Leave a concise summary and the next best step.

Current repo facts:
- Path: /opt/data/workspace/etl-scripting
- Remote: https://github.com/etdofreshai/etl-scripting.git
- GitHub token is available in /opt/data/.env as GH_TOKEN_ETDOFRESHAI
- Existing foundation includes lexer, parser, AST, semantic/type validation, CLI parse/check/run, and a minimal interpreter.

Preferred work order:
- tighten semantic checking and diagnostics
- improve interpreter coverage and correctness
- expand CLI surface coherently
- add IR lowering and textual IR output
- then add first asm/native codegen path
- later wasm and broader targets

Constraints:
- Work incrementally and safely.
- Prefer small, reviewable commits.
- Do not create noise commits.
- Do not overwrite or discard unrelated existing local changes.
- If the repo is temporarily busy or locked, back off cleanly.
- Before pushing, source /opt/data/.env and use GH_TOKEN_ETDOFRESHAI for authenticated push to origin main.

At the end of this turn, print exactly these sections:
what changed
latest commit hash if any
current test status
next most important step
session_id: <session id if available>
