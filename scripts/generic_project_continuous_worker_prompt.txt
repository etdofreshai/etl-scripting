Replace this project's minute-based autonomous cron loop with a persistent continuous-implementation worker plus a lightweight watchdog.

Fill in these project variables first:
- PROJECT_NAME: <project name>
- PROJECT_PATH: <absolute repo path>
- PROJECT_REMOTE: <git remote url>
- PUSH_TOKEN_ENV: <token env var name, if needed>
- MAIN_VERIFY_CMD: <primary verification command>
- WORK_PRIORITY: <ordered bullet list of near-term priorities>

Your job:
1. Remove or disable the current coding cron job for this project.
2. Create a persistent worker script in the project (for example under `scripts/`) that:
   - runs in a loop
   - resumes the same agent session between iterations when possible
   - works continuously in PROJECT_PATH
   - logs to a project-local log file
   - writes heartbeat/state/summary files for monitoring
   - avoids duplicate concurrent workers via pid or lock files
3. Create a lightweight watchdog script that:
   - runs separately from the worker
   - checks whether the worker is alive
   - restarts it if it dies
   - optionally restarts it if heartbeat becomes stale
   - is not itself the main implementation engine
4. Add helper scripts for:
   - start
   - status
   - stop
5. Start the watchdog as the tracked background process, letting it manage the worker.
6. Verify that:
   - watchdog is alive
   - worker is alive
   - logs are being written
   - summaries are readable
7. Print the exact commands for checking status, tailing logs, and stopping the loop.

Worker behavior requirements:
- continuously implement real improvements, not status churn
- prefer TDD when practical
- run MAIN_VERIFY_CMD before committing
- commit and push only meaningful, green increments
- avoid noise commits
- leave concise per-iteration summaries including:
  - what changed
  - latest commit hash if any
  - current test status
  - next most important step

Constraints:
- prefer small, reviewable commits
- source environment secrets if needed before pushing
- do not spawn overlapping workers
- if the repo is busy or locked, back off instead of interfering
- do not use cron as the primary work engine
